package com.apex.sandbox

import android.app.Application

class SandboxApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Reserved for future global initialization if needed
    }
}
