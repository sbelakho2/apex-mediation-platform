plugins {}

// Root build file for the ApexSandboxAndroid project.
// Android Studio can generate/upgrade the Gradle wrapper on first open.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
